{
  "name": "arjun",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:egress && npm run check:no-lora && tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "dev:auto": "node scripts/select-backend.mjs dev",
    "build:auto": "node scripts/select-backend.mjs build",
    "tauri:dev:gpu": "tauri dev --features cuda",
    "tauri:build:gpu": "tauri build --features cuda",
    "tauri:dev:vulkan": "tauri dev --features vulkan",
    "tauri:build:vulkan": "tauri build --features vulkan",
    "check:egress": "node scripts/check-egress.mjs",
    "check:deployment": "node scripts/check-deployment.mjs",
    "pack:offline": "node scripts/build-offline-pack.mjs",
    "rehearse:offline-install": "node scripts/rehearse-offline-install.mjs",
    "check:no-lora": "node scripts/check-no-lora.mjs",
    "check:offline": "node scripts/check-offline-build.mjs",
    "verify": "npm run check:egress && npm run check:no-lora && npm run check:deployment && npm run check:offline && npm run runtime:audit && npm run runtime:typecheck && npm run runtime:test && npm run test:ui && npm run runtime:build && npm run check:bundle:self && npm run check:bundle && npm run sbom && npm run check:ipc && npm run check:reachable && npm run check:targets && npm run check:lint-budget && npm run check:generated && npm run test:rust && npm run test:baseline && npm run test:integration && npm run test:sidecar && npm run check:whitespace",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml --lib",
    "test:sidecar": "python -m unittest discover -s sidecars/document_sidecar/tests",
    "accept": "node scripts/acceptance.mjs",
    "test:baseline": "cargo test --manifest-path src-tauri/Cargo.toml --test baseline",
    "rehearse": "node scripts/rehearse-no-wsl.mjs",
    "runtime:install": "npm --prefix agent-runtime ci --offline",
    "runtime:audit": "npm --prefix agent-runtime run audit",
    "runtime:typecheck": "npm --prefix agent-runtime run typecheck",
    "test:integration": "cargo test --manifest-path src-tauri/Cargo.toml --no-fail-fast --test agent_runtime --test two_runtimes --test rbac_isolation --test imported_skills_load --test test_memory_pipeline --test seed_local_accounts --test verify_certification_system --test scan_real_library --test artifact_formats_end_to_end",
    "test:rust:models": "cargo test --manifest-path src-tauri/Cargo.toml --no-fail-fast --test verify_llama_runtime --test verify_gui_execution_trace --test verify_real_world_model_switching -- --nocapture",
    "test:rust:all": "cargo test --manifest-path src-tauri/Cargo.toml --tests --no-fail-fast",
    "check:targets": "cargo check --manifest-path src-tauri/Cargo.toml --all-targets",
    "check:ipc": "node scripts/check-ipc.mjs",
    "check:reachable": "node scripts/check-reachable.mjs",
    "check:lint-budget": "node scripts/check-lint-budget.mjs",
    "check:generated": "node scripts/make-sbom.mjs && git diff --quiet --exit-code -- evidence/sbom.cdx.json evidence/sbom.md || (echo \"The committed SBOM is stale. Run npm run sbom and commit evidence/.\" && exit 1)",
    "check:whitespace": "git diff --check",
    "runtime:test": "npm --prefix agent-runtime test",
    "test:ui": "tsc -p tsconfig.test.json && node agent-runtime/node_modules/vitest/vitest.mjs run --config vitest.config.mjs",
    "runtime:build": "npm --prefix agent-runtime run build",
    "check:bundle": "node scripts/check-bundle.mjs",
    "check:bundle:self": "node scripts/check-bundle.test.mjs",
    "sbom": "node scripts/make-sbom.mjs"
  },
  "dependencies": {
    "@react-three/fiber": "^9.7.0",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "2.6.0",
    "@tauri-apps/plugin-log": "^2.9.0",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-sql": "^2.4.0",
    "lucide-react": "^1.28.0",
    "mermaid": "^11.17.2",
    "pdfjs-dist": "^6.3.289",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.18.2",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@types/three": "^0.185.4",
    "@vitejs/plugin-react": "^4.6.0",
    "tailwindcss": "^4.3.3",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
