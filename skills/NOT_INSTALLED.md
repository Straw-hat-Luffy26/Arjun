# Skills from the collection that are not installed

Each of these was read and left out. The reason is the point: a skill that
cannot do what it describes on this machine is worse than an absent one,
because a model offered it will try.

| Skill | Why not |
|---|---|
| `automation-audit-ops` | audits an agent harness, not plant work |
| `autoplan` | gstack CLI workflow, drives git and a browser |
| `brief` | changes how an agent writes, not a plant capability |
| `browse` | the web is the method; this build reaches no network |
| `careful` | guards a coding agent's shell, and ARJUN has no shell for it to guard |
| `deep-research` | multi-source web research is the method; ARJUN researches the organisation's own index with search_documents |
| `deployment-patterns` | cloud deployment; not what this product does |
| `design-consultation` | gstack CLI workflow, fetches fonts |
| `diagram` | drives an external renderer; ARJUN has create_diagram |
| `docker-patterns` | container build and registry work; an air-gapped plant workbench does none of it |
| `document-generate` | gstack CLI workflow |
| `document-release` | gstack CLI workflow |
| `documentation-lookup` | Context7 MCP is the method |
| `guard` | guards a coding agent's shell |
| `health` | gstack CLI workflow |
| `investigate` | gstack CLI workflow, drives a browser |
| `kubernetes-patterns` | cluster operations; not what this product does |
| `learn` | gstack CLI workflow |
| `make-pdf` | drives an external toolchain; ARJUN has create_pdf |
| `manim-video` | video generation; ARJUN produces no video |
| `nutrient-document-processing` | a different document stack (Nutrient/PSPDFKit API); ARJUN has its own artifact pipeline and would be misled |
| `office-hours` | gstack CLI workflow |
| `plan-canvas` | gstack CLI workflow, opens a browser |
| `plan-ceo-review` | gstack CLI workflow |
| `plan-eng-review` | gstack CLI workflow |
| `plan-orchestrate` | gstack CLI workflow |
| `prompt-optimizer` | for authoring agent prompts, not for plant work |
| `remotion-video-creation` | video generation; ARJUN produces no video |
| `review` | gstack CLI workflow, reads a git diff |
| `scrape` | the web is the method |
| `ship` | gstack CLI workflow, opens pull requests |
| `skill-comply` | audits a gstack skill tree |
| `skill-scout` | searches a remote skill registry |
| `skill-stocktake` | audits a gstack skill tree |
| `skillify` | authors gstack browser skills |
| `spec` | gstack CLI workflow |
| `strategic-compact` | manages a coding agent's own context |
| `visa-doc-translate` | a different OCR stack (EasyOCR/Tesseract/ReportLab); ARJUN has its own OCR |
